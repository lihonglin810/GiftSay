<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lanou3g.dllo.giftsay">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ui.app.GiftSayApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.LoginActivity" />
        <activity android:name=".ui.activity.SearchActivity" />
        <activity android:name=".ui.activity.WebActivity" />
        <activity android:name=".ui.activity.ColumnActivity" />
        <activity android:name=".ui.activity.StrategyRvDetailsActivity" />
        <activity android:name=".ui.activity.SingleDetailsActivity" />
        <activity android:name=".ui.activity.FillImageActivity" />
        <activity android:name=".ui.activity.StrategyLvDetailsActivity" />
        <activity android:name=".ui.activity.StrategyLvLookAllActivity" />
        <activity android:name=".ui.activity.CollectionActivity" />
        <activity android:name=".ui.activity.SingleWebActivity"></activity>
    </application>

</manifest>